-- Any variables outside the main functions are global!

-- Gets called when the computer starts up
function onLoad()
end

-- Gets called every tick
function onUpdate( deltaTime )
end

-- Gets called when the computer error's out.
function onError( errMsg )
end

-- Gets called when the computers code is saved.
function onReload()
end

-- Gets when the computer looses power
function onPowerLoss()
end

-- Gets called when the computer shuts down.
function onDestroy()
end