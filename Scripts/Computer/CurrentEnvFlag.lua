SCRAPCOMPUTERS_COMPUTER_CURRENT_ENV_FLAG = {
    -- This setting would make it safe or unsafe env depending on config
    ["Default"] = 0,

    -- This will make it safe env no matter what
    ["Safe Env"] = 1,

    -- This will make it unsafe env IF the current config env is set to unsafe env
    ["Unsafe Env"] = 2
}